clear all
image = imread('pictures\2022-02-26_14-08-02.jpg');
image = rgb2gray(image);
[bw,image]=maskBlack(image);
struct = strel("square",5);
bw = imopen(bw,struct);
length = sum(bw,2);
nonzero = nonzeros(length);
x = filter([1,1,1]/3,1,nonzero)
plot(nonzero);
%imshow(bw);